TASK: Test Runner Default Behavior

MUST DO:
1. Verify default behavior unchanged (no flags = runs simulator tests)
2. Verify --help works correctly
3. Verify --hardware flag works with HardwareController
4. Verify monkey-patching mechanism

DEFAULT BEHAVIOR:
- Without any flags, run_tests.py runs simulator tests
- Original code structure preserved:
  sim.start()
  try:
      sim.load()
      unittest.main('tests')
  finally:
      sim.shutdown()

--help OUTPUT:
usage: run_tests.py [-h] [--hardware] [--port PORT] [--flash FLASH] [--hard-reset]

Run integration tests against SimController or HardwareController

options:
  -h, --help         show this help message and exit
  --hardware, -H     Use HardwareController instead of SimController
  --port, -p PORT    Serial port for UART3 VCP (required with --hardware)
  --flash, -f FLASH  Optional: flash firmware before testing
  --hard-reset, -r   Use ST-Link hard reset between tests instead of software
                     reset

MONKEY-PATCHING MECHANISM:
1. When --hardware is set, create HardwareController instance
2. Monkey-patch: from util import controller; controller.sim = hw
3. Tests import 'sim' at module level: from util.controller import sim
4. Tests receive HardwareController instead of SimController
5. All test code works without modification

DEFAULT BEHAVIOR VERIFIED:
- No changes to original test structure
- Default mode is still simulator
- All original behavior preserved
