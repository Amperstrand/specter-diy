TASK: Test Runner --hardware Flag + Makefile Targets

MUST DO:
1. Add --hardware flag to use HardwareController
2. Add --port flag for serial port (/dev/ttyACM0 default)
3. Add --flash flag for optional firmware flashing
4. Add --hard-reset flag for ST-Link hard reset
5. Implement monkey-patching: util.controller.sim = hw_instance before tests import
6. Add Makefile targets: test-hil and test-hil-flash

IMPLEMENTATION SUMMARY:
- Modified test/integration/run_tests.py (20 lines → 88 lines)
  * Added argparse for flags: --hardware, --port, --flash, --hard-reset
  * Added run_hardware_tests() function
  * Added run_simulator_tests() wrapper
  * Implemented monkey-patching: controller.sim = hw
  * Added cleanup in finally block

- Modified Makefile (added 8 lines)
  * Added PORT ?= /dev/ttyACM0 variable
  * Added test-hil target: runs tests with --hardware
  * Added test-hil-flash target: runs tests with --hardware and --flash

KEY PATTERN:
When --hardware is set:
1. HardwareController(port=args.port) is created
2. from util import controller; controller.sim = hw
3. Tests import 'sim' from util.controller (module-level import)
4. Tests receive HardwareController instance instead of SimController
5. Tests run normally using the patched 'sim'
6. Cleanup: hw.shutdown(), then restore original controller

VERIFICATION:
1. --help shows all flags
2. No TODO/FIXME in modified files
3. Default behavior unchanged (no flags = simulator tests)
4. Monkey-patching mechanism implemented correctly

FILES MODIFIED:
- test/integration/run_tests.py (added 68 lines)
- Makefile (added 8 lines)
